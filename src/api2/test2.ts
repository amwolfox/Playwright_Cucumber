import { Given, Then } from '@cucumber/cucumber'

Given('the user is on the home page2', function () {
    // Code to simulate the user being on the home page
    console.log("User is on the home page");
});

Then('a message should be printed2', function () {
    // Print a simple message
    console.log("This is a simple message printed to the console.");
});